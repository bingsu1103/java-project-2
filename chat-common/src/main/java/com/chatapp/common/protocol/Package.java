package com.chatapp.common.protocol;

/**
 * Placeholder for protocol definitions.
 * Will contain: MessageType enum, Protocol class, etc.
 */
public class Package {
    // This file ensures the package directory is tracked by Git.
    // It will be removed once actual protocol classes are added.
}
